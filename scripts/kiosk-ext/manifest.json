{
  "manifest_version": 3,
  "name": "crt-tv kiosk startup flash fix",
  "description": "Keeps ws4kp hidden until it applies its kiosk-mode layout, so the CRT never shows the un-scaled page during startup.",
  "version": "1.0",
  "content_scripts": [
    {
      "matches": ["http://127.0.0.1/*", "http://localhost/*"],
      "css": ["hide.css"],
      "run_at": "document_start",
      "all_frames": true
    }
  ]
}
